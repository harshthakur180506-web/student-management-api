import { CommonCapitalLetterPipe } from './common-capital-letter.pipe';

describe('CommonCapitalLetterPipe', () => {
  it('should be defined', () => {
    expect(new CommonCapitalLetterPipe()).toBeDefined();
  });
});
